# Mock Mode (set to 'true' to use mock data instead of real API)
VITE_USE_MOCK_DATA=false

# API Configuration (using proxy - see vite.config.ts)
VITE_API_BASE_URL=/api
VITE_WS_URL=/ws

# Upload Configuration
VITE_MAX_UPLOAD_SIZE=500000000
VITE_SUPPORTED_FILE_TYPES=.pcap,.pcapng,.cap
